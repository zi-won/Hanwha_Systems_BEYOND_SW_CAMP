spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    jdbc-url: jdbc:mariadb://localhost:3306/jdbcdb
#    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
#    jdbc-url: jdbc:log4jdbc:mariadb://localhost:3306/jdbcdb
    username: root
    password: mariadb

# fatal -> error -> warn -> info -> debug -> trace
# fatal: ??????? ???? ?? ?? ??? ??
# error: ?? ?? ? ?? ??
# warn: ?? ????? ?? ??? ??? ?? ???
# info: ???, ?? ?? ?? ??? ???
# debug: ?? ? ??? ??? ???? ???
# trace: ??? ??? ???? ?? ? ? ??? ??
logging:
  level:
#    root: debug
    com:
      ohgiraffers:
        mybatisspring:
          section01:
            factorybean: debug
